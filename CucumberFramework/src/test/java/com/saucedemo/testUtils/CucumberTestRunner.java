package com.saucedemo.testUtils;

import io.cucumber.junit.platform.engine.Cucumber;

// This class serves as the entry point for running Cucumber tests.
// The @Cucumber annotation enables Cucumber to discover and execute feature files.
@Cucumber
public class CucumberTestRunner {
    // No code needed here as the annotation handles test execution
}